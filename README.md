# Move into your project folder (adjust path to where you saved the files)
cd smart-attendance-bunk-planner

# Initialize Git
git init

# Add all files
git add .

# Create your first commit
git commit -m "Initial commit: Smart Attendance & Bunk Planner"

# Rename branch to main
git branch -M main

# Connect to your GitHub repo (your username is Smalap)
git remote add origin https://github.com/Smalap/smart-attendance-bunk-planner.git

# Push everything
git push -u origin main
